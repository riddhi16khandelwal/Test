# Stage 1

## Approach

Notifications are ranked using:

Placement > Result > Event

For notifications of the same type,
the most recent notification is ranked higher.

## Complexity

O(N log N)

## Note

The provided API endpoint returned
HTTP 401 Unauthorized due to an invalid
authorization token. Therefore the
algorithm was validated using the
sample response format provided in
the assessment document.